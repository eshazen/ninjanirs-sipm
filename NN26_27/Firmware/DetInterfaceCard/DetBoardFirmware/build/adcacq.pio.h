// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ------ //
// adcacq //
// ------ //

#define adcacq_wrap_target 0
#define adcacq_wrap 8

#define adcacq_offset_acq_start 3u
#define adcacq_offset_reg_rw 9u

static const uint16_t adcacq_program_instructions[] = {
            //     .wrap_target
    0xf001, //  0: set    pins, 1         side 1     
    0x3003, //  1: wait   0 gpio, 3       side 1     
    0x3083, //  2: wait   1 gpio, 3       side 1     
    0xf000, //  3: set    pins, 0         side 1     
    0xe05e, //  4: set    y, 30           side 0     
    0x5001, //  5: in     pins, 1         side 1     
    0x0085, //  6: jmp    y--, 5          side 0     
    0x5001, //  7: in     pins, 1         side 1     
    0x9000, //  8: push   noblock         side 1     
            //     .wrap
    0xf001, //  9: set    pins, 1         side 1     
    0x90a0, // 10: pull   block           side 1     
    0xf000, // 11: set    pins, 0         side 1     
    0xf04f, // 12: set    y, 15           side 1     
    0x7101, // 13: out    pins, 1         side 1 [1] 
    0x5001, // 14: in     pins, 1         side 1     
    0x028d, // 15: jmp    y--, 13         side 0 [2] 
    0x9020, // 16: push   block           side 1     
    0x1009, // 17: jmp    9               side 1     
};

#if !PICO_NO_HARDWARE
static const struct pio_program adcacq_program = {
    .instructions = adcacq_program_instructions,
    .length = 18,
    .origin = -1,
};

static inline pio_sm_config adcacq_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + adcacq_wrap_target, offset + adcacq_wrap);
    sm_config_set_sideset(&c, 1, false, false);
    return c;
}

void adcacq_program_init(PIO pio, uint sm, uint offset, uint csn_pin, uint sclk_pin, uint miso_pin, uint mosi_pin) {
    pio_sm_config c = adcacq_program_get_default_config(offset);
    sm_config_set_set_pins(&c, csn_pin, 1);
    sm_config_set_sideset_pins(&c, sclk_pin);
    sm_config_set_in_pins(&c, miso_pin);
    sm_config_set_out_pins(&c, mosi_pin, 1);
    sm_config_set_in_shift(&c, false, false, 32); // shift left, autopush off
    sm_config_set_out_shift(&c, false, false, 32);
    sm_config_set_clkdiv_int_frac(&c, 2, 0);
    pio_gpio_init(pio, csn_pin);
    pio_gpio_init(pio, sclk_pin);
    pio_gpio_init(pio, miso_pin); // probably not strictly necessary
    pio_gpio_init(pio, mosi_pin); 
    //pio_gpio_init(pio, TRIG_PIN); // probably not strictly necessary
    pio_sm_set_consecutive_pindirs(pio, sm, csn_pin, 1, true);
    pio_sm_set_consecutive_pindirs(pio, sm, sclk_pin, 1, true);
    pio_sm_set_consecutive_pindirs(pio, sm, mosi_pin, 1, true);
    hw_set_bits(&pio->input_sync_bypass, 1u << miso_pin);
    pio_sm_clear_fifos(pio, sm);
    pio_sm_init(pio, sm, offset, &c); // load config into sm
}

#endif

