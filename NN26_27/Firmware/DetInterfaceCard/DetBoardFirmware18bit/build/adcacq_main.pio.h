// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

#define TRG_PIN 3

// ----------- //
// adcacq_main //
// ----------- //

#define adcacq_main_wrap_target 0
#define adcacq_main_wrap 9

#define adcacq_main_offset_acq_start 3u
#define adcacq_main_offset_reg_rw 10u

static const uint16_t adcacq_main_program_instructions[] = {
            //     .wrap_target
    0xf001, //  0: set    pins, 1         side 1     
    0x3003, //  1: wait   0 gpio, 3       side 1     
    0x3083, //  2: wait   1 gpio, 3       side 1     
    0xd035, //  3: irq    wait 5 rel      side 1     
    0xf000, //  4: set    pins, 0         side 1     
    0xe050, //  5: set    y, 16           side 0     
    0x5001, //  6: in     pins, 1         side 1     
    0x0086, //  7: jmp    y--, 6          side 0     
    0x5001, //  8: in     pins, 1         side 1     
    0x9000, //  9: push   noblock         side 1     
            //     .wrap
    0xf001, // 10: set    pins, 1         side 1     
    0x90a0, // 11: pull   block           side 1     
    0xf000, // 12: set    pins, 0         side 1     
    0xf04f, // 13: set    y, 15           side 1     
    0x7101, // 14: out    pins, 1         side 1 [1] 
    0x5001, // 15: in     pins, 1         side 1     
    0x028e, // 16: jmp    y--, 14         side 0 [2] 
    0x9020, // 17: push   block           side 1     
    0x100a, // 18: jmp    10              side 1     
};

#if !PICO_NO_HARDWARE
static const struct pio_program adcacq_main_program = {
    .instructions = adcacq_main_program_instructions,
    .length = 19,
    .origin = -1,
};

static inline pio_sm_config adcacq_main_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + adcacq_main_wrap_target, offset + adcacq_main_wrap);
    sm_config_set_sideset(&c, 1, false, false);
    return c;
}

void adcacq_main_program_init(PIO pio, uint sm, uint offset, uint csn_pin, uint sclk_pin, uint miso_pin, uint mosi_pin) {
    pio_sm_config c = adcacq_main_program_get_default_config(offset);
    sm_config_set_set_pins(&c, csn_pin, 1);
    sm_config_set_sideset_pins(&c, sclk_pin);
    sm_config_set_in_pins(&c, miso_pin);
    sm_config_set_out_pins(&c, mosi_pin, 1);
    sm_config_set_in_shift(&c, false, false, 32); // shift left, autopush off
    sm_config_set_out_shift(&c, false, false, 32);
    sm_config_set_clkdiv_int_frac(&c, 2, 0);
    pio_gpio_init(pio, csn_pin);
    pio_gpio_init(pio, sclk_pin);
    pio_gpio_init(pio, mosi_pin);
    pio_gpio_init(pio, miso_pin); // probably not strictly necessary 
    pio_gpio_init(pio, TRG_PIN);
    pio_sm_set_consecutive_pindirs(pio, sm, csn_pin, 1, true);
    pio_sm_set_consecutive_pindirs(pio, sm, sclk_pin, 1, true);
    pio_sm_set_consecutive_pindirs(pio, sm, mosi_pin, 1, true);
    hw_set_bits(&pio->input_sync_bypass, 1u << miso_pin);
    pio_sm_clear_fifos(pio, sm);
    pio_sm_init(pio, sm, offset, &c); // load config into sm
}

#endif

