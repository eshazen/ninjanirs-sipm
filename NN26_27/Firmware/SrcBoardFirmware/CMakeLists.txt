# ---  NinjaNIRS 2024  ---
# auto generated by Pico SDK
# modified by bzim@bu.edu

cmake_minimum_required(VERSION 3.13)
include(pico_sdk_import.cmake)

project(SrcBoardFirmware_project C CXX ASM)
set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)

pico_sdk_init()

add_executable(SrcBoardFirmware
  SrcBoardFirmware.c
)

pico_generate_pio_header(SrcBoardFirmware ${CMAKE_CURRENT_LIST_DIR}/led_sel_fsm.pio)

pico_enable_stdio_usb(SrcBoardFirmware 0)
pico_enable_stdio_uart(SrcBoardFirmware 0)

# prevent slow execution on first cycle
pico_set_binary_type(SrcBoardFirmware copy_to_ram)

target_link_libraries(SrcBoardFirmware
                      pico_stdlib
                      pico_unique_id
                      hardware_adc
                      hardware_pio
                      hardware_pll
                      hardware_clocks
                      hardware_spi
                      hardware_uart
                      )

pico_add_extra_outputs(SrcBoardFirmware)
