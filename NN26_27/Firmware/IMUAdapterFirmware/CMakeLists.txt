# ---  NinjaNIRS 2022  ---
# auto generated by Pico SDK
# modified by bzim@bu.edu

cmake_minimum_required(VERSION 3.13)
include(pico_sdk_import.cmake)

project(IMUAdapterFirmware_project C CXX ASM)
set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)

pico_sdk_init()

add_executable(IMUAdapterFirmware
  IMUAdapterFirmware.c
)

pico_generate_pio_header(IMUAdapterFirmware ${CMAKE_CURRENT_LIST_DIR}/uart_rx.pio)

pico_enable_stdio_usb(IMUAdapterFirmware 0)
pico_enable_stdio_uart(IMUAdapterFirmware 0)

# prevent slow execution on first cycle
pico_set_binary_type(IMUAdapterFirmware copy_to_ram)

target_link_libraries(IMUAdapterFirmware 
                      pico_stdlib
                      pico_unique_id
                      hardware_adc
                      hardware_pio
                      hardware_pll
                      hardware_clocks
                      hardware_i2c
                      )

pico_add_extra_outputs(IMUAdapterFirmware)
